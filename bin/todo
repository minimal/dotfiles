#! /bin/sh
regex="(TODO|XXX|FIXME)"
# check if rg is available
GREPPER=$(command -v rg || echo 'grep')
if [ $GREPPER != 'grep' ]; then
    $GREPPER $regex
else
    regex="\(TODO\|XXX\|FIXME\)"
    $GREPPER -R "$regex" *
fi
